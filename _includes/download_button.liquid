<style>
  /* al-folio 테마에 맞는 다운로드 버튼 스타일 */
  .download-btn {
    display: inline-flex;
    align-items: center;
    background-color: var(--global-card-bg-color, #ffffff);
    color: var(--global-theme-color, #0076df);
    border: 1px solid var(--global-divider-color, rgba(0,0,0,0.1));
    border-radius: 4px;
    padding: 8px 16px;
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s ease;
    box-shadow: 0 1px 2px rgba(0,0,0,0.1);
  }
  
  .download-btn:hover {
    background-color: var(--global-hover-color, #005cb2);
    box-shadow: 0 3px 5px rgba(0,0,0,0.2);
    color: var(--global-hover-text-color, #ffffff);
    transform: translateY(-1px);
    border-color: transparent;
  }
  
  .download-btn svg {
    margin-right: 8px;
    fill: var(--global-theme-color, #0076df);
  }
  
  .download-btn:hover svg {
    fill: var(--global-hover-text-color, #ffffff);
  }
  
  .format-badge {
    display: inline-block;
    background-color: var(--global-theme-color, #0076df);
    color: white;
    font-size: 11px;
    font-weight: 600;
    padding: 2px 6px;
    border-radius: 3px;
    margin-left: 8px;
    letter-spacing: 0.5px;
  }
</style>

<a href="{{ include.file_path | relative_url }}" class="download-btn" role="button" target="_blank">
  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16">
    <path d="M.5 9.9a.5.5 0 0 1 .5.5v2.5a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-2.5a.5.5 0 0 1 1 0v2.5a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2v-2.5a.5.5 0 0 1 .5-.5z"/>
    <path d="M7.646 11.854a.5.5 0 0 0 .708 0l3-3a.5.5 0 0 0-.708-.708L8.5 10.293V1.5a.5.5 0 0 0-1 0v8.793L5.354 8.146a.5.5 0 1 0-.708.708l3 3z"/>
  </svg>
  {% if include.button_text %}{{ include.button_text }}{% else %}Download {{ include.file_extension | upcase }}{% endif %}
  <span class="format-badge">{{ include.file_extension | upcase }}</span>
</a>
